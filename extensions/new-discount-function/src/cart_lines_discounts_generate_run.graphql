query CartInput {
  cart {
    lines {
      id
      cost {
        subtotalAmount {
          amount
        }
      }
    }
  }
  discount{
    discountClasses
    metafield(namespace: "FUNCTIONS", key: "SHOPIFY_FUNCTION") {
      jsonValue
    }
  }
}
