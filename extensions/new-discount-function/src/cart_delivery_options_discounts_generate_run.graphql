query DeliveryInput {
  cart {
    deliveryGroups {
      id
      deliveryOptions{
        cost{
          amount
        }
        handle
        title
      }
    }
  }
  discount{
    discountClasses
    metafield(namespace: "FUNCTIONS", key: "SHOPIFY_FUNCTION") {
      jsonValue
    }
  }
}
